/// From base of /mob/living/proc/update_turf_movespeed()
#define COMSIG_LIVING_UPDATE_TURF_MOVESPEED "update_turf_movespeed"
	/// Return value if the movespeed update is blocked
	#define TURF_MOVESPEED_BLOCKED (1<<0)
